<Window x:Class="Core.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Core"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800" Loaded="Window_Loaded">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Button Content="Create DB"  HorizontalAlignment="Left" Margin="264,260,0,0" VerticalAlignment="Top" Width="75" Click="Button_Click"/>
        <TreeView x:Name="CTreeWiew" HorizontalAlignment="Left" Height="187" Margin="10,40,0,0" VerticalAlignment="Top" Width="130">
            <!--<TreeView.ItemTemplate>
                <HierarchicalDataTemplate DataType="{x:Type self:MenuItem}" ItemsSource="{Binding Items}">
                    <TextBlock Text="{Binding Title}" />
                </HierarchicalDataTemplate>
            </TreeView.ItemTemplate>-->
        </TreeView>
        <Button Content="Add Data" HorizontalAlignment="Left" Margin="48,260,0,0" VerticalAlignment="Top" Width="75" Click="Button_Click_1"/>
        <ListBox x:Name="Lists" HorizontalAlignment="Left" Height="187" Margin="201,40,0,0" VerticalAlignment="Top" Width="165"/>

    </Grid>
</Window>
